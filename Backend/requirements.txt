# The Stationery — backend (FastAPI on Cloud Run)
fastapi>=0.115.0
uvicorn[standard]>=0.34.0

# Async Postgres (direct connection to Supabase via Supavisor pooler)
asyncpg>=0.30.0

# Settings & validation
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# Auth: verify Supabase-issued JWTs (ES256 via JWKS). [crypto] pulls cryptography.
pyjwt[crypto]>=2.9.0

# HTTP client for OpenRouter (OpenAI-compatible) and any outbound calls
httpx>=0.27.0

# Rate limiting (publish / reply / register)
slowapi>=0.1.9
